# Plan: init.md STEP 2 템플릿 → 체크리스트 방식으로 변경

## Context

현재 init.md의 STEP 2가 리터럴 마크다운 템플릿(30~102행)으로 CLAUDE.md 구조를 강제한다.
라우팅 구조, 데이터 플로우, 테이블 형식 등이 모든 프로젝트에 맞지 않아 유연성이 필요하다.

## 수정 대상

- `.claude/commands/init.md` — STEP 2, STEP 3, STEP 4

## 변경 상세

### 1. STEP 2 (26~102행) — 리터럴 템플릿 → 체크리스트 + 가이드

기존 ````markdown ... ```` 블록 전체를 아래 구조로 교체:

```
### STEP 2 — CLAUDE.md 작성

아래 섹션을 포함하여 CLAUDE.md를 작성한다.
프로젝트에 해당하지 않는 선택 섹션은 생략한다.
형식(테이블, 리스트 등)은 내용에 맞게 자유롭게 선택한다.

#### 필수 섹션

1. **문서 참조 블록** (`# CLAUDE.md` 바로 아래)
   - `docs/PRD.md` 존재 → 활성화 / 없으면 → TODO 주석
   - `docs/roadmaps/ROADMAP_v*.md` 존재 → 활성화 / 없으면 → TODO 주석
2. **프로젝트 개요** — 목적과 핵심 기능 1~2줄
3. **기술 스택** — 언어, 프레임워크, 주요 라이브러리
4. **명령어 (Scripts)** — 빌드/실행/테스트 명령어
5. **아키텍처** — 디렉토리 구조 필수, 하위 항목은 프로젝트에 맞게 구성
6. **개발 도구 및 설정** — 패키지 매니저, 런타임 버전, 포맷터/린터
7. **프로젝트 초기화 체크** — 아래 내용을 그대로 포함:
   (리터럴 블록)
8. **CLAUDE.md 유지 규칙** — 아래 내용을 그대로 포함:
   (리터럴 블록)

#### 선택 섹션 (해당 시에만)

- **환경 변수** — 없으면 생략
- **라우팅 구조** — 웹 앱이 아니면 생략
- **데이터 플로우** — 단순 프로젝트면 생략
```

### 2. STEP 3 (106~109행) — 제거

문서 참조 블록 활성화 로직이 STEP 2 필수 섹션 1번에 통합되므로 별도 STEP 불필요.

### 3. STEP 4 (113~137행) → STEP 3으로 번호 변경

내용은 변경 없이 번호만 STEP 3으로 변경.
